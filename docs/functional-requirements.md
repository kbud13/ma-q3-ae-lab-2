# Functional Requirements

This document defines the core functional requirements for the TODO app.

## Core Requirements

1. The user can add a due date to a task.
2. A task can be edited after it is created.
3. Tasks are displayed in a simple, consistent order.
   - Chosen rule: sort tasks by creation order (oldest first).
